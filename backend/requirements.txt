fastapi==0.115.6
uvicorn[standard]==0.34.0
websockets==14.1
httpx==0.28.1
python-dotenv==1.0.1
pydantic==2.10.4
pydantic-settings==2.7.1
asyncpg==0.30.0
slowapi==0.1.9
bleach==6.2.0

# Security — WebSocket token auth
python-jose[cryptography]==3.3.0

# Google Gemini AI (optional — primary AI engine when GEMINI_API_KEY is set)
google-generativeai==0.8.3

# Redis — caching + persistent WS sessions (optional — graceful fallback)
redis[asyncio]==5.2.1

# URL safety checking
validators==0.34.0
